import java.util.Stack;

public class test {
    String myString = "{test this is a test ( this is a test continued) ]";
    Stack<String> stack = new Stack<>();

    public Boolean method (String s) {

        for (int step = 0; step < myString.length(); step++) {
            if (myString[step].equals("(")) {
                
            }

            }
        }

        return true;
    }
}
